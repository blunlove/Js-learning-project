extends layout

block content
  #result
  #signIn
    #userName.message
      | 用户名:
      input#name(type="text")
    #passWord.message
      | 密  码:
      input#psw(type="password")
    br
    #button
      button#signin.button(onclick='check()') 登录
      button#register.button 注册
  script(src='/javascripts/checkuser.js')